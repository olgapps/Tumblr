<% @posts.each do |p|%>
    <div class="post_wrapper">
        <h2 class ="title"><%=link_to p.title, p %></h2>
        <p class="data"><%= time_ago_in_words p.created_at%> ago </p>

    </div>
<%end%>